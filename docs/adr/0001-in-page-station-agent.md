# Page Actions run in the tab

The Station Agent is in-page code that Shows a Section (Lenis scroll + highlight). It is not a computer-use robot that clicks pixels. A generic browser-use agent would ignore the landmark list, fight Lenis, and is a different product. Hands stay in the tab; the always-on box only returns a Show or a Clarification.
