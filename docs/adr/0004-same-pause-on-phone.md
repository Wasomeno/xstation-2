# Phone and desktop use the same Pause handle

A Command ends when the Visitor goes silent, on every device. Hold-to-talk and a phone-only send control were rejected so the Voice Surface stays one product. iOS Safari may fail to notice silence; that is accepted in this MVE and is the first thing to revisit if phone demos hang after speech.
